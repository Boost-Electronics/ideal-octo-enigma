<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sam Potts | Portfolio</title>
    <style>
        /* --- CORE SETUP --- */
        :root {
            --bg: #000c18;
            --text: #ffffff;
            --accent: #0070f3;
            --card-bg: #1e293b;
            --section-bg: #334155;
            --navy-top: #00162d;
            --sub-text: #94a3b8;
        }

        *, *::before, *::after {
            box-sizing: border-box;
        }

        body {
            margin: 0;
            padding: 0;
            height: 100vh;
            width: 100vw;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
            background: linear-gradient(to bottom, var(--navy-top) 0%, var(--bg) 50%, var(--section-bg) 100%);
            color: var(--text);
            overflow: hidden; /* Essential for the one-page layout */
            display: flex;
            flex-direction: column;
            -webkit-font-smoothing: antialiased;
        }

        /* --- THE 12PX BORDER FRAME --- */
        body::after {
            content: "";
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            border: 12px solid #1e293b; 
            pointer-events: none; 
            z-index: 9999;
        }

        /* --- LAYOUT WRAPPER --- */
        .viewport-wrapper {
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 40px 5%;
            z-index: 1;
        }

        /* --- IDENTITY SECTION --- */
        .section-intro {
            flex: 1.2;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .section-intro h1 {
            font-size: clamp(3rem, 8vw, 5rem);
            margin: 0 0 10px 0;
            letter-spacing: -2px;
            font-weight: 800;
            text-shadow: 0 0 30px rgba(0, 112, 243, 0.3);
            animation: fadeInHeader 1.2s ease-out;
        }

        .bio-wide {
            font-size: 1.1rem;
            color: #cbd5e1;
            max-width: 700px;
            line-height: 1.6;
            margin: 15px auto 0;
        }

        /* --- BUTTONS & DIVIDERS --- */
        .hero-btns {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
        }

        .btn {
            padding: 10px 30px;
            border: 2px solid var(--accent);
            color: #fff;
            text-decoration: none;
            font-weight: bold;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 6px;
            transition: all 0.3s ease;
            background: rgba(0, 112, 243, 0.05);
            backdrop-filter: blur(5px);
        }

        .btn:hover {
            background: var(--accent);
            box-shadow: 0 0 20px rgba(0, 112, 243, 0.4);
            transform: translateY(-2px);
        }

        .divider {
            width: 1px;
            height: 20px;
            background-color: #475569;
        }

        /* --- MIDDLE DIVIDER --- */
        .projects-header-wide {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            width: 100%;
            margin: 20px 0;
        }

        .projects-header-wide h2 {
            font-size: 0.9rem;
            letter-spacing: 6px;
            text-transform: uppercase;
            color: var(--accent);
            white-space: nowrap;
        }

        .line-long {
            flex-grow: 1;
            max-width: 300px;
            height: 1px;
            background-color: var(--accent);
            opacity: 0.3;
        }

        /* --- PROJECTS GRID --- */
        .section-projects-wide {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .projects-grid-full {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            width: 100%;
            max-width: 1200px;
        }

        .card-wide {
            opacity: 0;
            transform: translateY(30px);
            background: var(--card-bg);
            border: 1px solid #334155;
            padding: 40px;
            border-radius: 16px;
            text-decoration: none;
            color: white;
            transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
            display: flex;
            flex-direction: column;
            justify-content: center;
            min-height: 180px;
        }

        .card-wide h2 {
            font-size: 1.8rem;
            margin: 0 0 10px 0;
        }

        .card-wide p {
            color: var(--sub-text);
            font-size: 1rem;
            margin: 0;
            line-height: 1.5;
        }

        .card-wide:hover {
            border-color: var(--accent);
            background: #243147;
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        .card-wide.fade-in {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- ANIMATIONS --- */
        @keyframes fadeInHeader {
            from { opacity: 0; transform: translateY(-15px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* --- MOBILE RESPONSIVENESS --- */
        @media (max-width: 768px) {
            body { overflow-y: auto; height: auto; }
            body::after { border-width: 8px; }
            .viewport-wrapper { padding: 60px 25px; height: auto; }
            .projects-grid-full { grid-template-columns: 1fr; }
            .section-intro h1 { font-size: 2.5rem; }
        }
    </style>
</head>
<body>
    <div class="viewport-wrapper">
        
        <section class="section-intro">
            <header class="wide-header">
                <h1>Sam Potts</h1>
                <div class="hero-btns">
                    <a href="https://github.com/Boost-Electronics" class="btn" target="_blank">GitHub</a>
                    <span class="divider"></span>
                    <a href="https://www.linkedin.com/in/sam-potts-888158383/" class="btn" target="_blank">LinkedIn</a>
                </div>
                <p class="bio-wide">
                    Aspiring Electrical/Aerospace Engineer with a Focus on Innovative PCB Design and Flight Control Systems.
                </p>
            </header>
        </section>

        <div class="projects-header-wide">
            <span class="line-long"></span>
            <h2>Engineering Projects</h2>
            <span class="line-long"></span>
        </div>

        <section class="section-projects-wide">
            <div class="projects-grid-full">
                <a href="/portable-charger" class="card-wide">
                    <h2>Portable Charger</h2>
                    <p>Custom-Manufactured PCB Integration for 18650 Lithium Ion Batteries for a Portable Charging Solution.</p>
                </a>
                <a href="/future-projects" class="card-wide">
                    <h2>Future Projects</h2>
                    <p>Upcoming explorations in innovative PCB design, Efficient Electrical Systems, and Flight Controllers.</p>
                </a>
            </div>
        </section>

    </div>

    <script>
        // Intersection Observer for the Fade-In Effect
        const observerOptions = { threshold: 0.1 };
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('fade-in');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.card-wide').forEach((card, index) => {
            // Apply staggered delay via JavaScript
            card.style.transitionDelay = `${index * 0.15}s`;
            observer.observe(card);
        });
    </script>
</body>
</html>